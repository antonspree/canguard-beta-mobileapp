import React from "react";
import { JoinClubScreen } from "@/screens/guest";
import { Container } from "@/components";

const SingUp: React.FC = () => {
  return (
    <Container>
      <JoinClubScreen />
    </Container>
  );
};

export default SingUp;
