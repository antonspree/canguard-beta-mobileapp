import React from "react";
import { LoginScreen } from "@/screens/guest";
import { Container } from "@/components";

const SingUp: React.FC = () => {
  return (
    <Container>
      <LoginScreen />
    </Container>
  );
};

export default SingUp;
