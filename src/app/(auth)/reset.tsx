import React from "react";
import ResetScreen from "@/view/auth/ResetScreen";
import { SafeAreaView } from "react-native-safe-area-context";

const SingUp: React.FC = () => {
  return (
    <SafeAreaView>
      <ResetScreen />
    </SafeAreaView>
  );
};

export default SingUp;
